# Goal Focus Backend

Express + MongoDB backend scaffold for Goal Focus platform.

Quick start:

```bash
cd goal-focus-backend
npm install
cp .env.example .env
# set MONGO_URI and JWT_SECRET
npm run dev
```
